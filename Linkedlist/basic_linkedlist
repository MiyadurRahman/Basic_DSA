#include <iostream>
using namespace std;
struct node{
int data;
node* next;

};
int main(){
    node* head;
    node* second;
    node* third;
    head=new node();
    second=new node();
    third=new node();
    head->data=1;
    head->next=second;//linking first node to second
    second->data=2;
    second->next=third;//linking second node to third
    third->data=3;

    third->next=NULL;//ending the linked list
    //printing the linked list
    node* ptr=head;//why head?
}
